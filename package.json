{
  "name": "alltheplaces-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.js --bundle --minify --outfile=assets/js/bundle.min.js",
    "dev": "esbuild src/main.js --bundle --watch --outfile=assets/js/bundle.min.js"
  },
  "devDependencies": {
    "esbuild": "^0.19.8",
    "maplibre-gl": "^3.3.1",
    "@maplibre/maplibre-gl-geocoder": "^1.5.0",
    "pmtiles": "^3.2.0"
  }
}
