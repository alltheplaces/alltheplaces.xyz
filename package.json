{
  "name": "alltheplaces-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/map.js --bundle --minify --outfile=assets/js/map.min.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.2",
    "maplibre-gl": "^5.3.0",
    "@maplibre/maplibre-gl-geocoder": "^1.8.0",
    "pmtiles": "^4.3.0"
  }
}
